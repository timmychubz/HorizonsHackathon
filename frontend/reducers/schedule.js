import * as types from '../actions/types';

const initialState = [];

const schedule = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};

export default schedule;
